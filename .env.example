# frontend/.env
# Set this to your deployed Render backend URL.
# Once set, every device (phone, laptop, anywhere) uses the same cloud backend.
EXPO_PUBLIC_API_URL=https://ielts-daily-api.onrender.com/api
